export default (context) => {
  // if (context.app.$colorMode.value == 'light') {
  //   context.app.$cookies.set('color_mode', 'light')
  // } else if (context.app.$colorMode.value == 'dark') {
  //   context.app.$cookies.set('color_mode', 'dark')
  // }
}
